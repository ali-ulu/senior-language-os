{
  "name": "senior-language-os",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:packs": "node scripts/validate-packs.mjs",
    "test": "npm run check:syntax && npm run validate:packs",
    "check:syntax": "node --check app.js && node --check api/ai.js && node --check api/speech.js"
  }
}
